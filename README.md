# Web Push Server

Microservicio para enviar notificaciones Web Push con claves VAPID propias. Usable por instancias self-hosted del lector de manga/libros.

## Uso

1. Cloná el repo y generá tus claves (se puede usar PNPM):

```bash
npm install
npm run generate:vapid
```
